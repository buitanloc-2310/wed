# SKY FIRST NETWORK — official website

Production source for `skyfirst.io.vn`. This is one coherent Worker application rather than a collection of demo HTML files.

Run `npm install` and `npm run check`. Deployment instructions are in [docs/DEPLOY.md](docs/DEPLOY.md). The public information basis is seeded in `migrations/0002_seed_official_content.sql`; it contains 33 official questions in Vietnamese and English.
