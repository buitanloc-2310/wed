import { requireAdminToken, json } from '../../_auth.js';
export async function onRequestGet(context){const denied=await requireAdminToken(context);if(denied)return denied;const kind=new URL(context.request.url).searchParams.get('kind')||'';const r=await context.env.DB.prepare('SELECT id,kind,title,payload,created_at,updated_at FROM website_admin_records WHERE kind=? ORDER BY updated_at DESC').bind(kind).all();return json({ok:true,items:(r.results||[]).map(x=>({...x,...JSON.parse(x.payload||'{}')}))});}
export async function onRequestPost(context){const denied=await requireAdminToken(context);if(denied)return denied;const b=await context.request.json();if(!b?.id||!b?.kind||!b?.title)return json({ok:false,error:'Thiếu dữ liệu bắt buộc.'},{status:400});const {id,kind,title,...payload}=b;await context.env.DB.prepare("INSERT INTO website_admin_records(id,kind,title,payload,updated_at) VALUES(?,?,?,?,CURRENT_TIMESTAMP) ON CONFLICT(id) DO UPDATE SET kind=excluded.kind,title=excluded.title,payload=excluded.payload,updated_at=CURRENT_TIMESTAMP").bind(id,kind,title,JSON.stringify(payload)).run();return json({ok:true});}
export async function onRequestDelete(context){const denied=await requireAdminToken(context);if(denied)return denied;const b=await context.request.json();await context.env.DB.prepare('DELETE FROM website_admin_records WHERE id=?').bind(b?.id||'').run();return json({ok:true});}
