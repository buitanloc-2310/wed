import {json,requireAdminToken} from '../../_auth.js';import {firebaseSetPassword} from '../../_firebaseAdmin.js';
export async function onRequestPost(context){const denied=await requireAdminToken(context);if(denied)return denied;if(context.data.adminUser.role!=='developer')return json({ok:false,error:'Chỉ Quản trị hệ thống được đặt lại mật khẩu.'},{status:403});const b=await context.request.json().catch(()=>({})),uid=String(b.uid||''),password=String(b.password||'');if(!uid||password.length<8)return json({ok:false,error:'Mật khẩu mới phải có ít nhất 8 ký tự.'},{status:400});const target=await context.env.DB.prepare('SELECT role FROM website_admin_users WHERE uid=?').bind(uid).first();if(!target)return json({ok:false,error:'Không tìm thấy tài khoản.'},{status:404});if(target.role==='developer'&&uid!==context.data.adminUser.uid)return json({ok:false,error:'Không được đặt lại mật khẩu của Quản trị hệ thống khác.'},{status:403});try{await firebaseSetPassword(context.env,uid,password);await context.env.DB.prepare(`CREATE TABLE IF NOT EXISTS website_admin_audit(id INTEGER PRIMARY KEY AUTOINCREMENT,actor_uid TEXT NOT NULL,target_uid TEXT,action TEXT NOT NULL,detail TEXT,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run();await context.env.DB.prepare('INSERT INTO website_admin_audit(actor_uid,target_uid,action,detail) VALUES(?,?,?,?)').bind(context.data.adminUser.uid,uid,'admin.password_reset','').run();return json({ok:true})}catch(e){return json({ok:false,error:String(e.message||e)},{status:503})}}
