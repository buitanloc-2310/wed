document.addEventListener('DOMContentLoaded',()=>{const nav=document.querySelector('.main-nav'),toggle=document.querySelector('.menu-toggle');toggle?.addEventListener('click',()=>{const open=nav.classList.toggle('is-open');toggle.setAttribute('aria-expanded',String(open));});document.querySelectorAll('.nav-group').forEach(group=>{const trigger=group.querySelector('.nav-link');if(!group.querySelector('.dropdown'))return;trigger.addEventListener('click',event=>{if(innerWidth>780){event.preventDefault();const was=group.classList.contains('is-open');document.querySelectorAll('.nav-group.is-open').forEach(x=>x.classList.remove('is-open'));if(!was)group.classList.add('is-open');}});});document.addEventListener('click',event=>{if(!event.target.closest('.nav-group'))document.querySelectorAll('.nav-group.is-open').forEach(x=>x.classList.remove('is-open'));});});
