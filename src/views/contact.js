import { escapeHtml } from '../utils/escape.js';
import { tr } from '../services/i18n.js';

export function contactForm(ctx) {
  return `<section class="section tint"><div class="container narrow"><div class="eyebrow">${escapeHtml(tr(ctx,'contact.eyebrow','Gửi liên hệ'))}</div><h2>${escapeHtml(tr(ctx,'contact.title','Liên hệ với Sky First'))}</h2><form class="ajax-form" data-form-type="contact"><div class="form-grid"><label>${escapeHtml(tr(ctx,'contact.name','Họ và tên'))}<input name="name" required maxlength="120"></label><label>${escapeHtml(tr(ctx,'contact.email','Email'))}<input name="email" type="email" maxlength="160"></label><label>${escapeHtml(tr(ctx,'contact.phone','Số điện thoại'))}<input name="phone" maxlength="40"></label><label>${escapeHtml(tr(ctx,'contact.organization','Tổ chức/Đơn vị'))}<input name="organization" maxlength="180"></label></div><label>${escapeHtml(tr(ctx,'contact.message','Nội dung'))}<textarea name="message" required maxlength="3000" rows="6"></textarea></label><label class="check"><input name="consent" type="checkbox" required> ${escapeHtml(tr(ctx,'contact.consent','Tôi đồng ý để Sky First sử dụng thông tin này nhằm liên hệ và xử lý đề nghị.'))}</label><button class="btn primary" type="submit">${escapeHtml(tr(ctx,'contact.submit','Gửi liên hệ'))}</button><div class="form-status" aria-live="polite"></div></form></div></section>`;
}
