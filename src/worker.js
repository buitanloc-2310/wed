import { publicRoute } from './routes/public.js';
import { apiRoute } from './routes/api.js';
import { adminRoute } from './routes/admin.js';
import { text } from './core/http.js';
export default { async fetch(request,env,ctx) { const path=new URL(request.url).pathname; try { if(path.startsWith('/assets/')) return env.ASSETS.fetch(request); if(path==='/robots.txt') return text('User-agent: *\nAllow: /\nSitemap: https://skyfirst.io.vn/sitemap.xml\n'); if(path==='/sitemap.xml') return new Response(`<?xml version="1.0"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${['/home','/gioi-thieu','/hoat-dong','/don-vi','/tham-gia','/tin-tuc','/tra-cuu','/lien-he','/tai-tro-dong-hanh'].map(p=>`<url><loc>https://skyfirst.io.vn${p}</loc></url>`).join('')}</urlset>`,{headers:{'content-type':'application/xml'}}); if(path.startsWith('/api/')) return apiRoute(request,env); if(path.startsWith('/quan-tri')) return adminRoute(request,env); if(path.startsWith('/media/')) { const object=await env.MEDIA.get(path.slice(7)); return object?new Response(object.body,{headers:{'content-type':object.httpMetadata?.contentType||'application/octet-stream','cache-control':'public, max-age=86400'}}):new Response('Not found',{status:404}); } return publicRoute(request,env); } catch(error) { console.error('Sky First Worker error',error); return text('Hệ thống đang cần được kiểm tra. Vui lòng thử lại sau.',500); } } };
